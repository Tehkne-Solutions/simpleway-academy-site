# Como aplicar V8

1. Clonar/reabrir o repositório correto.
2. Limpar arquivos antigos.
3. Extrair este ZIP na raiz do repo.
4. Rodar `npm run build`.
5. Commit e push.
6. Cloudflare: Node 22, build `npm run build`, deploy `npm run deploy`, output `dist`.
